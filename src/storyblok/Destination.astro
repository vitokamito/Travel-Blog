---
  const { blok } = Astro.props
  console.log(blok)
---
 
<main class="prose-md prose prose-quoteless mx-6 pb-5 md:prose-lg md:mx-auto">
  <h1 class="text-6xl">{blok.title}</h1>
  <img src={blok.image} alt={blok.slug}>
  <p>{blok.main}</p>
</main>
